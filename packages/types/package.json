{
  "name": "@terminal/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types generated from Pydantic schemas",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
