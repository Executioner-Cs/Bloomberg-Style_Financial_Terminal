/**
 * Panel registry — every panel built in Phase 2+ is exported here.
 * Import panels from this barrel rather than from individual panel directories.
 */
export { ChartPanel } from './chart-panel';
