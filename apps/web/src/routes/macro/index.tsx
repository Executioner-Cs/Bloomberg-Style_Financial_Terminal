/**
 * Macro dashboard page — stub, implementation in Phase 6.
 */
import type { JSX } from 'react';

export default function MacroPage(): JSX.Element {
  return (
    <div
      style={{
        display: 'flex',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'var(--color-text-muted)',
        fontFamily: 'var(--font-mono)',
        fontSize: '12px',
      }}
    >
      {/* TODO(#9): Implement macro dashboard with FRED data */}
      MACRO · COMING SOON
    </div>
  );
}
