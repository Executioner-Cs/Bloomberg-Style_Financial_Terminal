/**
 * Screener page — stub, implementation in Phase 5.
 */
import type { JSX } from 'react';

export default function ScreenerPage(): JSX.Element {
  return (
    <div
      style={{
        display: 'flex',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'var(--color-text-muted)',
        fontFamily: 'var(--font-mono)',
        fontSize: '12px',
      }}
    >
      {/* TODO(#7): Implement screener panel with AG Grid */}
      SCREENER · COMING SOON
    </div>
  );
}
