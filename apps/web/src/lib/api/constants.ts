/**
 * API_V1_PREFIX — the version prefix shared by all REST endpoints.
 *
 * Centralised here so no route path is ever hard-coded as a string literal
 * scattered across the codebase. If the API version bumps, change it once here.
 */
export const API_V1_PREFIX = '/api/v1';
